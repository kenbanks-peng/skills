---
name: taskboard
description: Use when asked to perform work tasks associated with a project.
---

# Taskboard

If delegated a specific card or bounded work scope, read [worker instructions](references/worker.md). Otherwise, read [orchestrator instructions](references/orchestrator.md). A solo agent follows the orchestrator workflow and executes work directly.
