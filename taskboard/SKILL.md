---
name: taskboard
description: Use when asked to perform work tasks associated with a project.
---

# Taskboard

If acting as a worker assigned by an orchestrator, read [worker instructions](references/worker.md).

Otherwise, read [orchestrator instructions](references/orchestrator.md). A solo agent follows the orchestrator workflow and executes work directly.
